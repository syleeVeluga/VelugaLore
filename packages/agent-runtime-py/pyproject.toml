[project]
name = "weki-agents"
version = "0.0.0"
description = "WekiDocs pydantic-ai worker package"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
