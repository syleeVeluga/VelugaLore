[project]
name = "weki-agents"
version = "0.0.0"
description = "VelugaLore pydantic-ai worker package"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.0"]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
