id: curate-018
title: "design doc grew to cover three feature initiatives"
scope: wiki/design
fixture: fixtures/curate-018
agents_md_relevant_rules:
  - "design docs split when initiatives share no embedding overlap"
  - "each initiative gets its own design doc with shared parent"
invocation:
  slash: /curate scope:wiki/design
  mode: edit
expected_proposed_ops:
  - kind: split_doc
    count_min: 1
rationale_must_include:
  - "initiative"
  - "cosine"
post_invariants_after_apply:
  - "each split keeps its kind=design frontmatter"
  - "wiki/design/_index links every split"
post_invariants_after_revert:
  - "body_sha256 matches pre-run snapshot"
  - "splits collapsed into the original design doc"
decision: approve
expected_failure_mode: null
